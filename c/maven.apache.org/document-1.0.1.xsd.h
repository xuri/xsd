// Code generated by xgen. DO NOT EDIT.

typedef DocumentModel Document;

// DocumentModel is The meta data to construct a cover page for the document.
typedef struct {
	char OutputNameAttr; // attr, optional
	DocumentMeta Meta;
	DocumentTOC Toc;
	DocumentCover Cover;
} DocumentModel;

// DocumentTOC is TOC item.
typedef struct {
	char NameAttr; // attr, optional
	int DepthAttr; // attr, optional
	DocumentTOCItem Item[];
} DocumentTOC;

// DocumentTOCItem is A table of content item containing sub-items.
typedef struct {
	char NameAttr; // attr, optional
	char RefAttr; // attr, optional
	bool CollapseAttr; // attr, optional
	DocumentTOCItem Item[];
} DocumentTOCItem;

// Authors is The authors that appear on the cover page.
//             Only used if author is not given.
typedef struct {
	DocumentAuthor Author[];
} Authors;

// DocumentCover is The date as String (recommended format is ISO 8601) to appear on the cover.
//             Only used if <code>coverDate</code> is not set.
//             @since 1.1.1
typedef struct {
	char CoverTitle;
	char CoverSubTitle;
	char CoverVersion;
	char CoverType;
	char CoverDate;
	char Coverdate;
	Authors Authors;
	char Author;
	char ProjectName;
	char ProjectLogo;
	char CompanyName;
	char CompanyLogo;
} DocumentCover;

// DocumentAuthor is The state or province of the address of the author, if applicable.
typedef struct {
	char FirstName;
	char LastName;
	char Name;
	char Initials;
	char Title;
	char Position;
	char Email;
	char PhoneNumber;
	char FaxNumber;
	char CompanyName;
	char Street;
	char City;
	char PostalCode;
	char Country;
	char State;
} DocumentAuthor;

// KeyWords is A keyword pertaining to the document. The metadata can contain any
//             number of keyword elements, each element specifying one keyword.
typedef struct {
	char KeyWord[];
} KeyWords;

// DocumentMeta is The unique author of the document, usually as a String of "firstName lastName". For
//             more authors, you could use the &lt;authors/&gt; tag.
typedef struct {
	char Title;
	char Author;
	Authors Authors;
	char Subject;
	char Keywords;
	KeyWords KeyWords;
	char PageSize;
	char Generator;
	char Description;
	char InitialCreator;
	char Creator;
	char PrintedBy;
	char CreationDate;
	char Creationdate;
	char Date;
	char Modifydate;
	char PrintDate;
	char Printdate;
	DocumentTemplate Template;
	DocumentHyperlinkBehaviour HyperlinkBehaviour;
	char Language;
	int EditingCycles;
	int EditingDuration;
	DocumentStatistic DocumentStatistic;
	bool Confidential;
	bool Draft;
} DocumentMeta;

// DocumentTemplate is A template that was used to create the document.
typedef struct {
	char HrefAttr; // attr, optional
	char TitleAttr; // attr, optional
	char DateAttr; // attr, optional
	char ModifydateAttr; // attr, optional
} DocumentTemplate;

// DocumentStatistic is Statistical attributes of the document.
typedef struct {
	int PageCountAttr; // attr, optional
	int TableCountAttr; // attr, optional
	int DrawCountAttr; // attr, optional
	int ImageCountAttr; // attr, optional
	int ObjectCountAttr; // attr, optional
	int OleObjectCountAttr; // attr, optional
	int ParagraphCountAttr; // attr, optional
	int WordCountAttr; // attr, optional
	int CharacterCountAttr; // attr, optional
	int RowCountAttr; // attr, optional
	int FrameCountAttr; // attr, optional
	int SentenceCountAttr; // attr, optional
	int SyllableCountAttr; // attr, optional
	int NonWhitespaceCharacterCountAttr; // attr, optional
} DocumentStatistic;

// DocumentHyperlinkBehaviour is Specifies the default behavior for hyperlinks in the document.
typedef struct {
	char TargetFrameAttr; // attr, optional
} DocumentHyperlinkBehaviour;
